---
artifact: 01-requirements
owner: product-manager
version: v2
status: draft
supersedes: v1
created: 2026-09-12
reviewers: [architect, orchestrator]
gate: G-PM-01
---

# 需求挖掘报告 · 极境 ZenithLens

> 唯一输入：`docs/PROJECT_BRIEF.md`（OpenCode 独立重做版，status=confirmed），**重点 §9 的 40 项 P0 强制组件**。
> 设计基准（只参照样式）：`docs/00-charter/00-design-reference-legacy-v2.html`。
> 本报告只写"要什么"，不写"怎么实现"；技术选型留给架构师。
> **v2 变更（skill 驱动重跑 S1）**：在 v1 的 20 条需求四段式之上，补入 **用户画像与用户故事（user-stories）**、
> **RICE/ICE/Kano 定级依据（prioritization-frameworks）**、**预演摘要（pre-mortem，全量落 `02-prd.md §6`）**；
> 旧版原样备份为 `01-requirements-v1.md`，需求条目不删只增（新增 R-21 特调组件组）。

## 摘要

1. 用户是**一个人、一台 Windows、22 只场外基金、每天看一次**，要的是"今天该不该动"的一句话判断，不是更多图表。
2. 三大核心问题：**方向判断**（涨/跌 + 可信度 + 组合覆盖）、**实时估值保真**（五档降级 + 来源徽章 + 估值≠净值）、**可审计**（任何判断可回溯、台账只增不改）。
3. 立身之本 = **可验证的预测**：跑不赢基线 `max(恒涨,恒跌)` 就自动灰显，绝不把"看起来能预测其实不能"的资产标为可信。
4. 本报告收敛出 **21 条需求**（v1 的 20 条 + v2 新增 R-21 特调组件组，含 Brief §9 的 40 项 P0 逐组件要求），其中 P0 = 14 条、P1 = 6 条、P2 = 1 条。
5. 已剔除用户提的"解法型愿望"（如"加更多图表""接 LLM 做预测"），只保留露出真场景的条目；**40 项特调组件为 v2 升版触发项**（用户强制、全部 P0）。

## 正文

### 0. 方法论与 skill 使用声明（v2 新增）

> 纪律：外部 skill 只提供**方法、检查项、话术**，不改变本体系工件的四段式/四要素/不做清单/P0-P1-P2 结构与门禁判据（C:\Users\10719\Documents\Default Project\.opencode\protocols\capability-map.md §0）。

| skill | 本次用途 | 落到本报告哪一节 |
|---|---|---|
| `create-prd`（只取方法） | 用其 8 段分析清单（问题/目标/细分/价值主张/方案/假设）**补全**需求背景与假设，**不采用**其模板替换我们的结构 | §1 背景假设、§2 用户画像 |
| `user-stories` | 3C（Card/Conversation/Confirmation）+ INVEST 分解用户旅程，为每条旅程补可测验收 | §3.5 用户故事 |
| `prioritization-frameworks` | 用 ICE + RICE + Kano 给 P0/P1/P2 定级并给依据，消除"都挺重要" | §4 优先级定级 |
| `pre-mortem` | 假设上线失败反推风险，分老虎/纸老虎/大象并给处置 | §6 预演摘要（全量见 `02-prd.md §6`） |

**自检降级说明**：skill 中"询问用户"的交互步骤在本体系子 agent 无法直接执行，已降级为自检清单与 `say` 上报候选（capability-map §0 纪律 2）。

### 1. 需求挖掘方法与背景假设

#### 1.1 连问三层「为什么」

以用户最常提的一句诉求演示：

```
用户说：我想看昨天的收益
→ 为什么？        想知道整体今天是赚了还是亏了
→ 为什么现在看不到？要手动打开 App 一只只点
→ 为什么要一只只点？持仓 22 只，没有汇总
→ 真需求：打开就能知道"整体今天是赚是亏、哪几只拖后腿"
```

**判定规则**：没有「场景」和「痛点」的条目一律算愿望，不进本报告。
下方每条需求均标注来源（Brief 章节号 = 用户书面诉求）。

#### 1.2 背景与待验证假设（create-prd 方法补全）

| # | 假设 | 被谁验证 | 若证伪的后果 |
|---|---|---|---|
| A1 | 用户每天只看得懂"一句话结论"，不需要更多图表 | §3.5 US-01、US-06 | 需求重心从"判断"滑向"可视化" |
| A2 | 场外基金官方净值公布前，盘中估算/重仓加权足以支撑 T+1 决策 | R-02/R-03 验收 | 估值保真价值下降，五档链需重新定级 |
| A3 | 组合加权只统计"开口仓位"能真实反映可执行性 | R-09 验收 | 加权口径需改为全仓位 |
| A4 | 用户愿意为"诚实灰显"接受更少的信号数量 | R-10、Brief §8 | 产品失去立身之本 |
| A5 | 特调组件（§9）不是装饰，而是用户已习惯的工作界面 | R-21、Brief §9 引言 | 删组件=用户回到旧工作流，产品被弃用 |

### 2. 用户画像与旅程（user-personas 方法）

**主画像 P1 · 自我审计型个人投资者（唯一用户）**
- 环境：Windows 单机、本地不联网部署、每日收盘后看一次。
- 持仓：22 只场外基金（含 QDII，见 Brief §5.3）。
- 双身份：**使用**预测信号的人 + **审计**这套系统是否诚实的人。
- 成功感受：打开就知道今天该不该动，且每个数字都知道"它凭什么"、"它可信吗"。
- 失败感受：被一个看起来很像真的数字骗了；或必须回到 22 个 App 里一只只点。

**核心旅程（决定需求边界）**
1. 早晨/收盘 → 看组合总览 → 判断整体赚亏与覆盖度（US-01、US-03、US-04）。
2. 看到某行涨跌 → 追问"这是官方还是估算" → 展开来源徽章（US-02）。
3. 今天有信号 → 点开信号 → 逐步看它凭什么 → 决定是否持有/赎回（US-05、US-06）。
4. 月度回看 → 核对预测跑不跑得赢基线 → 判断系统值不值得信（US-07）。
5. 事后核对 → 翻台账看某天到底出了什么、结果如何（US-08）。

### 3. 需求清单

> 每条 = `[场景] → [痛点] → [需求] → [验收]`（四段式，缺场景/痛点不算需求）。

---

### A 组 · 看懂持仓与估值（对应 Brief §3.2、§5.4）

#### R-01 · 打开即见组合总览
- **优先级**：P0
- **来源**：PROJECT_BRIEF §1「每天需要『今天该不该动』的判断依据」
- **场景**：用户持 22 只场外基金，每天收盘后打开产品，第一眼想知道"整体今天赚了还是亏了"。
- **痛点**：现在要一个个打开基金 App 点开每只基金，22 只点完才拼出全貌，无法一眼判断。
- **需求**：打开首页即同时看到 **总资产 / 今日预估盈亏 / 累计收益 / 收益率** 四个汇总数字。
- **验收**：首页首屏渲染完成后，一屏之内同时可见上述 4 个数字；总资产 = Σ(份额 × 最新估值)，与逐行明细求和误差为 0；4 个数字均可点开查看口径说明（分子/分母来源）。

#### R-02 · 实时估值五档降级保真
- **优先级**：P0
- **来源**：PROJECT_BRIEF §3.2「官方净值 / 盘中估算 / 重仓加权 / 代理近似 / 未知 五档降级链」
- **场景**：用户盘中/盘后想看某只基金今天的涨跌，但场外基金官方净值要到很晚才公布。
- **痛点**：官方净值没出时只能看估算，而估算值和真实净值长得一模一样，不标注就会把"猜的"当成"既成事实"。
- **需求**：每只基金今日涨跌按 **五档降级链** 取数：official（官方净值）→ intraday（盘中估算）→ holdings（重仓加权）→ proxy（代理近似）→ unknown（未知）；降级顺序由后端决定，前端只如实呈现。
- **验收**：任意持仓行的估值来源徽章必须等于后端返回的 `mode` 字段，不得静态硬编码；降级到 unknown 时该行不显示任何今日涨跌数值；同一基金同日只能处于一档。

#### R-03 · 估值 ≠ 净值，来源可区分可追溯
- **优先级**：P0
- **来源**：PROJECT_BRIEF §3.2「估值 ≠ 净值，两者来源必须可区分、可追溯」
- **场景**：用户想核对"这个数是官方公布的，还是系统算的"。
- **痛点**：估算与净值混在一个字段里，事后无法追溯当时用的是哪一档、经过什么降级路径。
- **需求**：估值（今日预估值）与净值（已公布官方值）是两个独立字段；每个估值行附**来源徽章 + 降级链路**，可展开查看。
- **验收**：数据模型中估值与净值字段名不同且互不覆盖；徽章悬浮/展开可见降级链路（如 official 未出 → 尝试 intraday → 命中）；对同日同一基金，官方净值回填后不得改写当日已入账的估值记录来源。

#### R-04 · 未知 ≠ 持平
- **优先级**：P0
- **来源**：PROJECT_BRIEF §5.4「未知 ≠ 持平」
- **场景**：某只基金今天没有任何可信估值来源。
- **痛点**：若渲染成 `+0.00%`，用户会读成"今天没涨没跌"，把"没有数据"说成"没有波动"。
- **需求**：无可信估值时，涨跌列渲染为「未知」、今日盈亏列渲染为「—」，且汇总处显式声明未覆盖只数。
- **验收**：当某行 pct 为空/未知，界面**绝不出现** `+0.00%`；汇总卡标注"仅覆盖 N/M 只，其余未知不计入"；全量未知时今日盈亏显示「未知」而非 0。

#### R-05 · 覆盖度分级说明（A/B/C/D）
- **优先级**：P0
- **来源**：PROJECT_BRIEF §4「把『看起来能预测其实不能』的资产标为可信 → 比不做更糟」；§8「宁可只覆盖少数资产但每个数字都 trustworthy」
- **场景**：用户想知道 22 只里哪些信号可信、哪些只是占位。
- **痛点**：不区分就把未验证资产和已验证资产一视同仁，等于把"猜的"混进结论。
- **需求**：每只基金标注覆盖档位 **A 已接入 / B 代理已验证 / C 代理待验证 / D 暴露待定**，并给出每档依据与只数/市值/占比。
- **验收**：首页可见四档分级面板，含每档只数、市值、占比；C/D 档在信号列显示「待验证」/「暴露待定」，不得显示为可信信号；分级依据可展开阅读。

#### R-06 · 隐私打码
- **优先级**：P1
- **来源**：PROJECT_BRIEF §5.4「隐私打码（隐藏金额）」
- **场景**：用户在有人在场时打开产品。
- **痛点**：金额直接暴露。
- **需求**：一键隐藏所有金额（总资产/盈亏/持仓金额），占比与涨跌幅保留。
- **验收**：打开开关后所有金额位渲染为 `••••`；占比与百分比涨跌仍可见；刷新后开关状态保持（本地记忆）。

---

### B 组 · 方向判断与预测（对应 Brief §3.1、§4、§5.1、§5.2）

#### R-07 · 单只基金方向预测信号
- **优先级**：P0
- **来源**：PROJECT_BRIEF §3.1「对能预测的资产给出未来 N 天涨/跌方向 + 可信度」；§5.1 预测核心逻辑
- **场景**：用户想知道某只已接入基金明天（T+1）是涨还是跌。
- **痛点**：没有可验证的方向判断，只能凭感觉。
- **需求**：按 **E1 跳空 / E3 无量急跌触发门 → DDSM 状态模型 → 双否决（拥挤度 + KLOW2）→ 出口 P > 0.52** 输出"涨"信号；不满足则"灰（不表态）"，**永不做空**。
- **验收**：每条信号必须同时给出触发门名称、P 值、否决层状态；`P ≤ 0.52` 一律不输出"涨"；任一否决亮灯输出"弃权"；界面无任何"做空/卖空"入口。

#### R-08 · 信号全链路可解释
- **优先级**：P0
- **来源**：PROJECT_BRIEF §3.3「任何一次判断可回溯（凭什么、用的什么数据）」
- **场景**：用户看到一个"涨"信号，想知道凭什么。
- **痛点**：黑盒信号无法建立信任。
- **需求**：每条信号可展开显示 **6 步链路**：①触发门 ②DDSM 档位 ③拥挤度否决 ④KLOW2 否决 ⑤出口判定 ⑥输出。
- **验收**：展开后 6 步齐全，且每步带具体数值（如 `ret −3.82%`、`量比 0.72`、`P=0.61`、`z 分位`、`下影线比`）；数值缺失的步骤显示"数据缺失"而非留空。

#### R-09 · 组合层三窗口加权预测
- **优先级**：P0
- **来源**：PROJECT_BRIEF §3.1「组合层给『组合加权 P + 覆盖市值占比』，覆盖不足必须显式声明」
- **场景**：用户想知道"整体今天能不能动"。
- **痛点**：单只信号没有组合视角，无法决策。
- **需求**：给出 **T+1 / T+3 / T+15** 三窗口组合加权 P，规则为：**只有开口仓位计入权重**（灰不是 0.5，是从分子分母同时剔除）、**C/D 档不参与**、**必须同时显示覆盖市值占比**。
- **验收**：每个窗口同时显示「组合加权 P」与「覆盖市值占比」两个数字，且标注覆盖金额；当覆盖市值 < 100% 时，界面显式写出"其余 X% 不表态"；三窗口缺基线数据时标注"待填充"。

#### R-10 · 预测成绩对照基线（报 Δ 而非裸命中率）
- **优先级**：P0
- **来源**：PROJECT_BRIEF §4「T+1 方向正确率不跑赢基线（基线 = max(恒涨, 恒跌)）→ 产品无价值」；§5.1「报增量 Δ，不报裸命中率」
- **场景**：用户想知道这套预测到底有没有价值。
- **痛点**：裸命中率可以被高基线掩盖（例如全市场恒涨 55%，模型 56% 其实没本事）。
- **需求**：任何成绩展示都必须与**同期基线 `max(恒涨, 恒跌)`** 并列，主指标为增量 **Δ**。
- **验收**：复盘页同时显示「命中率 / 基线 / 开口数 / Δ」四列；Δ 旁标注基线口径来源；无基线数据的窗口显示"待填充"，**不得用估计值填充**。

#### R-11 · 走前验证与冻结 holdout（方法论约束）
- **优先级**：P0
- **来源**：PROJECT_BRIEF §5.2「走前时序 CV（禁随机切分）· purge gap = H+2 · placebo ≥2pp · 冻结 holdout 60 日禁调参」
- **场景**：用户在审计这套系统本身是否诚实。
- **痛点**：随机切分/事后调参会制造虚高回测，无法区分真本事与过拟合。
- **需求**：所有回测成绩必须来自**走前时序 CV**，purge gap = H+2，冻结 holdout 60 日不参与调参，新结论须过 placebo ≥2pp。
- **验收**：复盘页成绩卡标注"走前 CV"字样与 purge gap 口径；holdout 窗口在调参期间冻结、界面不得出现 holdout 调参痕迹；任何新因子结论附 placebo 检验值。

#### R-12 · 审计台账 INSERT-ONLY + 到期回填
- **优先级**：P0
- **来源**：PROJECT_BRIEF §3.3「台账 INSERT-ONLY」；§5.2「台账 INSERT-ONLY」
- **场景**：用户事后想核对某天到底出了什么信号、结果如何。
- **痛点**：台账能被改 = 事后诸葛，无法证伪。
- **需求**：每次开口在**收盘时点固定写盘、只增不改**，到期自动回填 T+1/T+3/T+15 实际涨跌。
- **验收**：台账每条记录含 `date / code / gate / p_up / ddsm_state / veto_crowd / veto_klow2 / signal / t1_real / t3_real / t15_real`；界面无"编辑/删除台账"入口；同一天同一 code 不可重复写入；回填只写 `t*_real` 字段，不动 `signal`。

#### R-13 · 三窗口口径定义与可执行路径说明
- **优先级**：P1
- **来源**：PROJECT_BRIEF §3.1「未来 N 天」；§5.3「QDII 的 T+2 是信息优势不是缺陷」；设计基准分析页口径
- **场景**：用户看到"明日涨 58%"会误以为当天就能买入。
- **痛点**：场外基金 15:00 截止申购、信号 15:30 才出，口径不说清会误导操作。
- **需求**：明确定义 T+1 为 `close[T+1] > close[T]`，并说明三种执行路径（A 新建仓位 / B 已持有仓位 / C 场内 ETF）中哪条可执行。
- **验收**：口径定义可展开阅读；明确写出"对场外基金最现实的用法是 B（持有/赎回决策）"；不得出现暗示"当天可买入"的文案。

---

### C 组 · 治理、操作与工程底线

#### R-14 · 因子治理可视化
- **优先级**：P1
- **来源**：PROJECT_BRIEF §8「再做覆盖度 B/C/D 档扩展与因子治理」
- **场景**：用户想审计因子是否在健康地进化/淘汰。
- **痛点**：因子增删无留痕，无法判断"哪个因子真的有用"。
- **需求**：展示四盘（正式/淘汰影子/有效因子影子/冷冻）、四本账本、评分链与生命周期规则。
- **验收**：四个盘面各自可见因子列表与角色；至少可见 4 本账本的用途与最近记录；评分链的悬崖规则与窗口权重可见。

#### R-15 · 引擎观测台
- **优先级**：P1
- **来源**：PROJECT_BRIEF §1「审计这套系统本身是否诚实」；设计基准引擎页
- **场景**：用户想知道引擎此刻在不在线、台账写到哪了。
- **痛点**：引擎黑盒，出问题无从察觉。
- **需求**：只读展示引擎状态 HUD、影子盘台账状态与最近记录、DDSM 状态表、冻结参数、六维输入快照。
- **验收**：HUD 展示运行状态与最近更新时间；台账记录显示字段清单；DDSM 冻结参数（K=4 / p=0.25 / MIN_N=15 / SHRINK=10 / BAND=0.02）可见且标注"改动须重走六关"；本页只读无编辑入口。

#### R-16 · 数据源健康可见
- **优先级**：P1
- **来源**：PROJECT_BRIEF §5.3「行情主通道：腾讯…实时估值走腾讯/天天基金/新浪」（接口随时可能被墙）
- **场景**：某天估值异常，用户要判断是数据源挂了还是引擎坏了。
- **痛点**：数据源不可见，故障无法定位。
- **需求**：设置页展示各数据源连通状态、最近成功时间、失败次数。
- **验收**：每个数据源显示 正常/不通/波动 三态之一 + 最近时间戳；不通常见时首页出现降级提示条。

#### R-17 · 持仓录入与加减仓
- **优先级**：P1
- **来源**：PROJECT_BRIEF §2「持 22 只场外基金」；§5.3 持仓读 `app_data/funds_data.json`
- **场景**：买入/卖出后需要更新持仓。
- **痛点**：持仓数据与产品脱节。
- **需求**：支持录入/编辑基金（代码、名称、金额或份额、成本、确认天数 T+1/T+2/T+3）、加仓/减仓、删除（二次确认）。
- **验收**：基金代码校验为 6 位数字；金额与份额至少填一项；删除需二次确认；改动后总览与明细同步刷新，数值与持仓数据一致。

#### R-18 · 启动可靠性（Python 3.8+ / 桌面 exe）
- **优先级**：P0
- **来源**：PROJECT_BRIEF §4「Python 3.8+ 跑不起来 / exe 点『启动』无反应 → 交付失败」；§5.3「必须能在 Python 3.8+ 直接 `python app.py` 起服」
- **场景**：用户双击启动产品或执行起服命令。
- **痛点**：以前版本出现过启动无反应、版本不兼容。
- **需求**：交付物无需第三方依赖即可在 Python 3.8+ 起服；桌面 exe 点击"启动"须有明确反馈。
- **验收**：在 Python 3.8 环境下执行 `python app.py` 可成功起服并访问 `http://127.0.0.1:<port>`；exe 点击"启动"后 ≤10s 内出现窗口或明确的错误提示（不得静默无反应）；启动失败须在界面/日志给出可读原因。

#### R-19 · 本地离线与数据不出本机
- **优先级**：P0
- **来源**：PROJECT_BRIEF §2「本地部署不联网」；§7「本地单用户 web + 桌面 exe，不联网部署」
- **场景**：用户在意隐私与离线可用。
- **痛点**：联网产品有数据外泄与断网不可用风险。
- **需求**：本地单用户部署，数据只存本机；除行情/估值取数外不发生外部上传。
- **验收**：设置"关于"明确声明本地存储、数据不出本机、不构成投资建议；无多用户/登录/云同步入口；断网时已缓存数据仍可查看。

#### R-20 · LLM 编码器（shadow 观察区，非预测）
- **优先级**：P2
- **来源**：PROJECT_BRIEF §5.5「事件维接入 DDSM（三窗口无增量）」；设计基准设置页 LLM 接口
- **场景**：用户预留事件编码能力，但明确它不做最终预测。
- **痛点**：把未验证的事件维当生产结论会污染预测。
- **需求**：LLM 仅用于**事件编码**，输出必须先过四关回测才能进生产；当前停在 shadow/观察区，界面明确标注。
- **验收**：界面标注"仅用于事件编码，不做最终预测 / 当前 shadow 观察区"；温度、输出格式等硬约束可见；事件维未过回测前不得出现在信号链中。

#### R-21 · Brief §9 特调组件全量保留（v2 新增 · 用户强制）
- **优先级**：P0
- **来源**：PROJECT_BRIEF §9「必须保留的特调组件（用户强制，2026-09-12）」；§9 引言「上一版 S1 线框丢失 legacy v2 壳 83% 的特调内容」。
- **场景**：用户已习惯 legacy v2 壳的操作界面（7 Tab、估值徽章、因子四盘、125 格立方体、竞技场矩阵等），S1 线框若缺项，用户回到旧工作流时找不到入口。
- **痛点**：上一版 S1 把 §9 的 40 项当成"可选项"丢了大半，导致线框只是"好看的成功态"，无法承载真实工作流；且组件缺项会让下游契约缺字段、前端只能自造数据（触失败定义"前端假数据"）。
- **需求**：Brief §9.1~§9.8 列出的 **40 项特调组件全部按 P0 保留**（持仓 6 / 信号 3 / 分析 4 / 因子 6 / 引擎 8 / 复盘 4 / 设置 5 / 交互状态 4），逐项落入 PRD（`02-prd.md §2.5` R-UI-01..40）、UI 设计（`03-ui-design.md §7`）、线框（`04-ui-wireframe.html`）与契约字段。
- **验收**：40 项与 Brief §9 一一对应，**缺一即 G-FE-01 不通过**（Brief §9.9）；每项在 PRD 含"组件名/所属页/输入-处理-输出-异常/验收标准/P0"；线框可实际打开并逐项点击；契约须为立方体/竞技场/六维/因子治理/时序链提供结构化字段（Brief §9.9 末条）。

---

### 3.5 用户故事（user-stories skill · 3C + INVEST）（v2 新增）

> 采用 **3C**：Card（一句话标题）/ Conversation（意图）/ Confirmation（可测验收）。
> 每条通过 **INVEST**（独立/可协商/有价值/可估/小/可测）。设计链接统一指向 `04-ui-wireframe.html` 对应页。

**US-01 · 打开即知整体赚亏**（→ R-01，持仓页）
- Card：作为一个每天看一次的用户，我想打开就同时看到总资产/今日盈亏/累计收益/收益率，以便一眼判断今天整体赚亏。
- Conversation：22 只逐个点开太慢；我要的是"汇总结论"而非"更多行"。
- Confirmation：① 一屏同时可见 4 个数字；② 总资产=Σ(份额×估值) 且与明细求和误差=0；③ 未知持仓显示"仅覆盖 N/M 只"而非计入；④ 每卡可点开看口径。
- INVEST：独立（不依赖其他故事可先做）；可测（误差=0）。

**US-02 · 分清"官方净值"与"系统估算"**（→ R-02/R-03，持仓页）
- Card：作为一个要核对数字来源的用户，我想看到每行涨跌的来源徽章并展开降级链路，以便不被估算冒充净值骗到。
- Conversation：估算和历史净值长得一样，必须能追溯当时用的哪一档。
- Confirmation：① 徽章文案=后端 `mode`，无硬编码；② 五档各有独立文案且同屏可区分；③ 展开可见降级链路+历史 MAE+方向命中率+样本数；④ 官方净值回填不改写当日来源。
- INVEST：可测（逐行比对 mode）；有价值（立身之本）。

**US-03 · 该未知就显示未知**（→ R-04，持仓页）
- Card：作为一个怕被"假 0"误导的用户，我想在无数据时看到"未知/—"，以便不把没数据读成没波动。
- Conversation：`+0.00%` 与"未知"必须视觉语义都不同。
- Confirmation：① pct 空绝不出现 `+0.00%`；② 今日盈亏显示"—"；③ 汇总注明未知不计入；④ 全未知时显示"未知"。
- INVEST：小、可测（负向断言）。

**US-04 · 知道哪些基金可信**（→ R-05，持仓页）
- Card：作为一个审计者，我想看到 A/B/C/D 四档分级与依据，以便只信 A/B 档信号。
- Conversation：未验证资产不能混进结论。
- Confirmation：① 四档各显只数/市值/占比；② C/D 信号列显示"待验证/暴露待定"，不得显示"涨"；③ 组合加权只纳入 A/B（可断言）。
- INVEST：可测。

**US-05 · 看懂一条信号凭什么**（→ R-07/R-08，信号页）
- Card：作为一个不信任黑盒的用户，我想展开信号的 6 步链路并看到每步数值，以便判断这条"涨"是否可信。
- Conversation：触发门→DDSM→双否决→出口，每一步都要有数字。
- Confirmation：① 6 步齐全；② 每步带具体数值；③ 缺数据显示"数据缺失"不留空；④ 无任何做空入口。
- INVEST：可测（步骤齐全+数值）。

**US-06 · 判断整体今天能不能动**（→ R-09，分析页）
- Card：作为一个要决定持有/赎回的用户，我想看三窗口组合加权 P 与覆盖市值占比，以便知道这条判断覆盖了多少钱。
- Conversation：灰仓位不算 0.5，是从分子分母剔除；C/D 不参与。
- Confirmation：① 每窗口同显加权 P 与覆盖占比；② 覆盖<100% 显式写"其余 X% 不表态"；③ 无开口显示"—"；④ C/D 可断言不在加权集合。
- INVEST：可测。

**US-07 · 判断这套预测到底有没有价值**（→ R-10/R-11，复盘页）
- Card：作为一个审计者，我想看到命中率与同期基线并列并给出 Δ，以便识别"被高基线掩盖"的假本事。
- Conversation：只报裸命中率等于骗人；成绩必须来自走前 CV。
- Confirmation：① 同显命中率/基线/开口/Δ 四列；② Δ 标注基线口径与 CV 口径（purge=H+2）；③ 基线缺失显示"待填充"不填估计值；④ 回测与实盘可区分。
- INVEST：可测。

**US-08 · 事后核对某天信号**（→ R-12，复盘页）
- Card：作为一个事后追责的用户，我想翻台账看某天出了什么信号、结果如何，以便验证它没被事后修改。
- Conversation：台账只增不改才能被证伪。
- Confirmation：① 记录含完整字段；② 无编辑/删除入口；③ 同日同 code 唯一；④ 回填只写 `t*_real`。
- INVEST：可测。

### 4. 优先级定级（prioritization-frameworks skill）（v2 新增）

> **原则**：优先排"问题/机会"，不排"功能"（skill 核心原则）。先对每条需求做 **Kano 归类**（理解期望），再用 **ICE** 做快速排序，关键项用 **RICE** 复核（单一用户时 Reach 恒为 1，故 RICE 主要看 Effort 与 Impact）。
> 评分口径：Impact（对"今天该不该动"的价值，0–10）· Confidence（信心，0–1）· Ease（实现易度，1–10）· Effort（人日，粗略）。
> 单用户产品 **Reach 固定 = 1**，因此 `RICE = (1 × I × C) / E` 仅用于把"高代价低价值"项压下去；P0/P1/P2 主要由 Kano + ICE 决定。

| 需求 | Kano 归类 | I（价值） | C（信心） | E（易度） | ICE=I×C×10×E | Effort(人日) | RICE=(I×C)/Effort | 定级 | 依据 |
|---|---|---|---|---|---|---|---|---|---|
| R-01 组合总览 | Must-be | 9 | .95 | 8 | 684 | 2 | 4.28 | **P0** | 没有它用户无法一眼判断（Brief §1、§4"数字错误"红线） |
| R-02 五档降级 | Must-be | 10 | .85 | 5 | 425 | 6 | 1.42 | **P0** | 估值保真是三大核心问题之一（Brief §3.2）；信心受外部源稳定性影响 |
| R-03 估值≠净值 | Must-be | 9 | .9 | 7 | 567 | 3 | 2.7 | **P0** | 失败定义"展示数字与后端不一致"直接拦截项 |
| R-04 未知≠持平 | Must-be | 9 | .98 | 9 | 794 | 1 | 8.82 | **P0** | 成本最低、防误导收益最高，Brief §5.4 明文 |
| R-05 覆盖度 A/B/C/D | Must-be | 9 | .85 | 7 | 535 | 3 | 2.55 | **P0** | 失败定义"把不能预测的标为可信"拦截项 |
| R-06 隐私打码 | Performance | 6 | .9 | 9 | 486 | 1 | 5.4 | **P1** | 体验项，不影响"判断"正确性 |
| R-07 单只方向预测 | Must-be | 10 | .8 | 4 | 320 | 8 | 1.0 | **P0** | 产品立身之本；E 低、C 受方法论约束，但不可缺 |
| R-08 信号 6 步链路 | Must-be | 9 | .9 | 7 | 567 | 3 | 2.7 | **P0** | "可审计"三大核心之一，Brief §3.3 |
| R-09 三窗口组合加权 | Must-be | 9 | .85 | 6 | 459 | 4 | 1.91 | **P0** | Brief §3.1 覆盖不足必须声明 |
| R-10 成绩 vs 基线报 Δ | Must-be | 10 | .9 | 8 | 720 | 2 | 4.5 | **P0** | 失败定义"不跑赢基线=无价值"拦截项 |
| R-11 走前 CV / holdout | Must-be | 9 | .85 | 5 | 383 | 5 | 1.53 | **P0** | 防过拟合是诚实性前提（Brief §5.2） |
| R-12 台账 INSERT-ONLY | Must-be | 9 | .95 | 7 | 599 | 2 | 4.28 | **P0** | Brief §3.3；可被改则无法证伪 |
| R-13 三窗口口径说明 | Performance | 7 | .9 | 9 | 567 | 1 | 6.3 | **P1** | 防误操作，属体验/说明层 |
| R-14 因子治理 | Performance | 7 | .8 | 5 | 280 | 6 | 0.93 | **P1** | Brief §8 放"P0 闭环之后"，审计增强 |
| R-15 引擎观测台 | Performance | 6 | .85 | 6 | 306 | 4 | 1.28 | **P1** | 审计增强，非每日决策必需 |
| R-16 数据源健康 | Performance | 6 | .9 | 8 | 432 | 2 | 2.7 | **P1** | 故障定位，提升可维护性 |
| R-17 持仓录入/加减仓 | Performance | 6 | .9 | 6 | 324 | 4 | 1.35 | **P1** | 数据维护入口，非预测链 |
| R-18 启动可靠性 | Must-be | 9 | .95 | 7 | 599 | 2 | 4.28 | **P0** | 失败定义"起不来=交付失败" |
| R-19 本地离线 | Must-be | 8 | .98 | 9 | 706 | 1 | 7.84 | **P0** | Brief §2/§7 明文边界 |
| R-20 LLM 事件编码 | Attractive | 4 | .3 | 6 | 72 | 5 | 0.24 | **P2** | 乐观型价值、信心低、当前 shadow，不可进生产 |
| R-21 Brief §9 特调组件 | Must-be | 10 | .9 | 4 | 360 | 10+ | 0.9 | **P0** | 用户强制、缺一 G-FE-01 不通过（Brief §9.9）；Effort 高但不可裁 |

**定级规则（可复现）**：
1. **P0 = Must-be ∩（失败定义拦截项 ∩ Brief §8 P0 闭环）**。凡触碰 Brief §4 任一失败定义，或属"持仓+实时估值+信号/预测+复盘"最小闭环，一律 P0，不得因 Effort 高而降级（如 R-07/R-21）。
2. **P1 = Performance 属性**：做对了提升体验/审计完整性，缺失不直接导致产品无价值（R-06/R-13/R-14/R-15/R-16/R-17）。
3. **P2 = Attractive 且 信心低**：锦上添花、未验证、当前必须 shadow（R-20）。
4. **ICE 异常高但降级的**：R-14（ICE 280）/R-15（306）虽 E 尚可，但 Kano 为 Performance 且非失败定义拦截项 → 保持 P1。
5. **不允许"都挺重要"**：每条只有一个定级；争议走 `say` 上报主 Agent。

### 5. 需求优先级汇总

| 优先级 | 需求 | 数量 |
|---|---|---|
| **P0** | R-01, R-02, R-03, R-04, R-05, R-07, R-08, R-09, R-10, R-11, R-12, R-18, R-19, R-21 | 14 |
| **P1** | R-06, R-13, R-14, R-15, R-16, R-17 | 6 |
| **P2** | R-20 | 1 |

> 说明：P0 = 没有就不上线（Brief §8「P0 最小闭环：持仓 + 实时估值 + 信号/预测 + 复盘」+ 启动可靠性 + 离线性 + §9 特调组件）；P1 = 影响体验/审计完整性；P2 = 锦上添花。

### 6. 预演摘要（pre-mortem skill）（v2 新增）

> 全量预演（老虎/纸老虎/大象 + launch-blocking 处置）落 `docs/00-charter/02-prd.md §6`，此处只列 3 只**必须开工前处置的老虎**：

| # | 老虎（真实风险） | 分类 | 处置方向 | Owner |
|---|---|---|---|---|
| T1 | 外部行情源（腾讯/天天/新浪）失效或改口径 → 五档降级链整体哑火，估值保真崩塌 | Launch-Blocking | 契约定"超时阈值 + 降级原子性 + 缓存兜底"；首页降级条 | architect |
| T2 | 40 项特调组件信息密度过高（125 格立方体/竞技场矩阵/六维 z-bar）→ 前端重画时再次丢项 | Launch-Blocking | 线框逐项可点击 + PRD 逐项验收 + 前端按 R-UI 清单建组件 | frontend-dev |
| T3 | 冻结 holdout 起始时点未与排期绑定 → 回测成绩被事后调参污染，立身之本失效 | Launch-Blocking | `06-system-arch` 明确起始日，台账 INSERT-ONLY 固化 | architect |

### 7. 与失败定义（Brief §4）的对应关系

| 失败定义 | 拦截需求 |
|---|---|
| T+1 不跑赢基线 → 无价值 | R-10（报 Δ）、R-11（走前 CV） |
| 把不能预测的标为可信 | R-05（A/B/C/D）、R-07（P≤0.52 不开口） |
| 数字错误 | R-01（口径）、R-17（校验） |
| 展示数字与后端不一致 | R-02（徽章由后端驱动）、R-03（字段分离） |
| 前端假数据冒充真实 | R-02、R-15（只读观测）、R-16（数据源可见） |
| Python 3.8+/exe 起不来 | R-18 |
| 特调组件缺项（Brief §9） | R-21（逐项 P0、缺一即 G-FE-01 不通过） |

## 自验收

- [x] 每条需求都能追溯到 Brief 或一句用户原话（来源列已标章节）
- [x] 每条需求含「场景 + 痛点 + 需求 + 验收」四段
- [x] 区分「真需求」与「用户提的解法」：LLM 预测、更多图表等解法未进需求清单（R-20 明确降为 shadow）
- [x] 优先级仅 P0/P1/P2，无"都重要"（§4 每条单一评级 + Kano/ICE/RICE 依据）
- [x] 验收均可测量（含数值/字段名/时间阈值）
- [x] 无「用户可能需要…」式无来源推测条目
- [x] 覆盖 Brief §3 三大核心问题（方向判断 R-07~R-13 / 实时估值 R-02~R-03 / 可审计 R-08、R-12）
- [x] 覆盖 Brief §5 约束（§5.1 R-07、§5.2 R-11、§5.3 R-16~R-18、§5.4 R-04~R-06、§5.5 R-20）
- [x] **v2**：覆盖 Brief §9 的 40 项 P0 特调组件（R-21 逐项对账）
- [x] **v2**：使用 user-stories skill 产出 8 条 3C/INVEST 用户故事（§3.5）
- [x] **v2**：使用 prioritization-frameworks skill 给出 Kano/ICE/RICE 定级表与可复现规则（§4）
- [x] **v2**：使用 pre-mortem skill 给出预演摘要（§6，全量落 PRD §6）
- [x] 未指定任何技术选型/框架

## 下游交接

- **主消费方**：`architect`。需其执行：对 P0 需求逐条评估实现代价与可行性，产出 `05-product-arch` / `06-system-arch`，并在 `10-arch-review` 中就"五档降级的取数顺序与失败回退""DDSM 状态空间的存储与重训代价"提出实质分歧。
  - **v2 追加**：契约必须为 R-21（Brief §9）涉及的立方体/竞技场/六维/因子治理/时序链提供结构化字段（Brief §9.9 末条）。
- **次消费方**：`qa`。消费 R-10/R-11/R-12 的验收口径制定测试用例（尤其 Δ 对照基线与台账不可改）；按 R-21 逐项建验收用例。
- **残留风险**：
  1. R-02 五档降级依赖外部行情源稳定性，官方净值公布时点不确定 → 需架构师给出"估值延后回填但不改写当日来源"的落地方式（责任人：architect，期限：S2 定稿前）。
  2. R-11 冻结 holdout 60 日的起始时点需与开发排期绑定，尚未确定 → 由架构师在 `06-system-arch` 明确（责任人：architect，期限：S2）。
  3. R-18 的 Python 3.8 兼容边界（标准库版本差异）需开发组长在构建时实测（责任人：dev-lead，期限：S5）。
  4. **v2 新增**：R-21 的 40 项中，R-UI-22/25/26（竞技场/六维/立方体）需要契约先给数组字段，否则前端自算→造假风险（责任人：architect，期限：S2）。
- **未覆盖**：多用户、云端同步、实盘交易（Brief §7 明确不做，列入 PRD 不做清单）。
